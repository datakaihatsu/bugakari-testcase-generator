# ロードマップ：汎用テストケース生成エンジンへの道

作成: 2026-05-21
最終更新: 2026-06-01 (フェーズ2.7: 絞り込みアプローチ採用・02/03フィードバック完全対応)

---

## ビジョン

> **旧JSON + 新JSON を投入するだけで、列形式テストケースCSVが自動生成される**

プロジェクトごとに改修される10〜30工種を対象に、差分抽出 → 乖離チェック → テスト計画 → 列形式CSV生成 の4ステップを自動化する。

---

## アーキテクチャ（①→①.5→②→③）

```
旧JSON + 新JSON
    ↓
① 差分抽出（決定論的・完全自動）
    → step1.0_差分レポート.csv

    ↓ 修正方針.txt と照合

①.5 乖離チェック（キーワード+カテゴリ照合）
    → step1.5_乖離チェック.csv  (緩和一致率 平均96%)

    ↓

② テスト計画生成（JSON解析+yaml業務ルール）
    → step2.0_テスト計画.csv  (vary/auto/fix軸の分類)

    ↓

③ 列形式テストケースCSV生成（Expression評価器+flow_walker）
    → step3.0_テストケース.csv  (列形式・期待値計算済)
```

実行コマンド:

```
python 共通/pipeline.py <old_json> <new_json> <output_dir>
```

修正方針.txt が new_json と同じディレクトリにあれば自動的に ①.5 を実行。

---

## 進化の経緯

```
Phase 1 (2026-05-21〜26): 工種ごとに手書きバッチ
  問題: 同じ工種を再実行しても同じ出力が保証されない

Phase 2 (2026-05-27): ①②③パイプライン v1
  JSON → ① 差分抽出 → ② テスト提案(差分起点) → ③ テキスト形式CSV
  限界: Excelフィルタ不能・期待値は「変化があること」止まり

Phase 2.1 (2026-05-28): 列形式CSV対応 ✅
  Expression評価器 (Being.Core.Expressions準拠) を Python実装
  flow_walker でフロー走査(AutoSelectJoken連鎖評価)
  yaml axis_behaviors で業務知識を最小限で表現
  ②③ 完全書き直し → 列形式CSV出力
  32-4527 で手作り仕様書と完全一致 (4 TC × 13列)

Phase 2.5 (2026-05-28): 乖離チェック ①.5 追加 ✅
  修正方針.txt と step1 を照合する step1_5_check を実装
  緩和一致率 75〜80% 目標 → 実測 96% で目標越え

Phase 2.6 (2026-05-29): リスク評価機構 ✅
  Claudeリスク評価/コメント/最終判定 を step1.5 に統合
  claude_risk_judgement.yaml で Claude判定を蓄積・自動適用
  8工種すべて「[OK] 実質的に問題なし」達成 (最終一致率100%)

Phase 2.7 (2026-06-01): 絞り込みアプローチ採用 + 02/03 フィードバック完全対応 ✅
  ★ アーキテクチャ大改修: 名称マッチ(v1) → SitsumonNo 直接活用 + forward 経路探索(v2)
  step2/step3 を全面書き直し (旧版は 共通/_backup_v1/ に保管)
  A〜I 全 9 課題完了:
    A   到達しない/UI不可視な Sit を列除外
    A-1 ShortCutSitsumonNo 持ち Sit を除外 (参照表示用)
    A-2 AutoSelectJoken に MinKigou を含む内部分岐 Sit を除外
    B   SitsumonKind=17 (数値入力型) を「任意」 表記
    C   列順を SitsumonNo 順 → フロー登場順
    D   選択肢ラベルを数値表記化 (VarName 持ち列も表示候補)
    E   絞り込みアプローチ (差分起点 vary 軸自動推論 + 前段 fix 強制)
    F   期待値 0 のとき任意入力含むなら「計算結果が正しいか」
    G   差分/回帰判定 + 業務ルール状態戻し回帰TC自動生成
    H   「選択肢の適切さ確認」に規格名計上の観点追加
    I   display_col の unique 値優先選択

  実証: 02 大型ブレーカで「2014年規制」 自動推論、32-4527 で上段→下段→上段 状態戻し回帰TC生成
  業務担当者向け資料: 進捗報告/アーキテクチャ比較_提案前後.html

Phase 3 (次): 他工種展開とフィードバック収集
  04/05/06/07 で業務担当者からフィードバック収集
  必要に応じて Phase 2.7 で確立した v2 ロジックの調整
  08 薬剤(Kg) JSON 投入後に動作確認
```

---

## フォルダ構成（2026-05-28 時点）

```
14.歩掛Jsonからテストケース作成可能か/
│
├── 工種別/<工種番号_工種名>/
│   ├── input/                   旧JSON / 新JSON / 修正方針.txt
│   ├── output/                  step1〜step3 + step1.5 + テストケース仕様書(手作り)
│   ├── spec/                    context_確定仕様.md(一部工種)
│   └── scripts/                 generate_testcases.py(一部工種・Phase 1遺産)
│
├── 共通/
│   ├── engine/                  共通基盤コンポーネント
│   │   ├── bugakari_json.py     BugakariJSON + KeisanHyo再export
│   │   ├── expression.py        ★Expression評価器 (Being.Core.Expressions準拠)
│   │   └── flow_walker.py       ★FlowItems走査・AutoSelect連鎖・代価表行フラグ
│   ├── knowledge/               ★業務知識
│   │   └── global_rules.yaml    axis_behaviors他
│   ├── step1_diff/extract_diff.py           ①差分抽出
│   ├── step1_5_check/check_alignment.py     ★①.5 乖離チェック
│   ├── step2_proposals/generate_proposals.py ②テスト計画
│   ├── step3_csv/generate_csv.py             ③列形式CSV
│   ├── pipeline.py                          ①→①.5→②→③ 連続実行
│   ├── source_ref/                          積算ソフト参照ソース (初回参照のみ・git管理外)
│   ├── scripts/                             Phase 1遺産
│   └── spec/汎用テストケース生成仕様.md
│
├── docs/
│   ├── backlog/                 棚上げ・将来検討
│   ├── タスク一覧.md
│   ├── ロードマップ_汎用テストケース生成エンジンへの道.md（本ファイル）
│   ├── 工種選定候補_260525.md
│   └── 着手順序.txt
│
└── 進捗報告/                    進捗Markdown・報告資料 (git管理外)
    ├── パイプライン構成図.md    (★.mermaid から .md 化)
    ├── 進捗_2026-05-21.md 〜 28.md
    └── 歩掛JSONテストケース自動生成_報告.pptx
```

★ = 2026-05-28 で新規追加・大幅変更

---

## ① 差分抽出 (完了)

`共通/step1_diff/extract_diff.py` v1から変更なし。

検出対象6カテゴリ・8工種で検証済み。

---

## ①.5 修正方針との乖離チェック (2026-05-28追加)

`共通/step1_5_check/check_alignment.py`

入力: `step1.0_差分レポート.csv` + `修正方針.txt`
出力: `step1.5_乖離チェック.csv` (一致率・検出漏れの監視)

### ロジック

1. 修正方針.txt をパース(【】カテゴリ・親子インデント集約)
2. 各項目からキーワード抽出(英数字識別子・質問No・数値・日本語キーワード・動詞)
3. step1 各行とカテゴリ・キーワードで照合
4. score>=2 = matched / score=1 = partial / カテゴリのみ一致 = category_partial / それ以外 = unmatched

### 緩和一致率 (平均96% / 8工種)

| 工種 | 緩和一致率 |
|---|---|
| 01〜03, 05, 06, 07, 32-4527 | 100% |
| 04 下地処理工 | 67% |
| 02 大型ブレーカ | 100% |

### 機械検出不可な残部

- 「資料通りに変更」「適切な位置へ配置」のような曖昧表現
- 業務概念の言語ギャップ (「労務」 vs `S1A`)
- これらは Claude が業務文脈で補足する運用に回す → Phase 2.6 で構造化

---

## ①.6 リスク評価機構 (2026-05-29追加)

`共通/step1_5_check/check_alignment.py` 拡張。

### 新規出力カラム

| カラム | 内容 |
|---|---|
| Claudeリスク評価 | 妥当 / 軽微 / 重大 / (空欄) |
| Claudeコメント | 判定理由 |
| 最終判定 | OK / 要対応 |

### 動作

1. pipeline 実行時に `<output_dir>/claude_risk_judgement.yaml` を読み込み (無ければスキップ)
2. unmatched / category_partial 行に対して `intent_body` 前方一致・包含一致で判定反映
3. 妥当/軽微 → 最終判定 OK、重大 → 最終判定 要対応
4. サマリー行に「最終一致率」「最終結論」を出力
   - `[OK] 実質的に問題なし`
   - `[要対応] 重大リスク N件`
   - `[一部未評価] N件のClaude評価が未入力`

### 達成サマリー（8工種すべて [OK]）

| 工種 | 機械(緩和) | Claudeリスク | 最終一致率 | 最終結論 |
|---|---|---|---|---|
| 01 回航費 | 100% | - | 100% | [OK] |
| 02 大型ブレーカ | 100% | - | 100% | [OK] |
| 03 送気用設備 | 100% | - | 100% | [OK] |
| 04 下地処理工 | 67% | 軽微1 | **100%** | [OK] |
| 05 コンクリート吹付機 | 100% | - | 100% | [OK] |
| 06 土のう積工 | 100% | - | 100% | [OK] |
| 07 インターロッキング | 100% | - | 100% | [OK] |
| 32-4527 裏込砕石工 | 100% | - | 100% | [OK] |

---

## ② テスト計画生成 (Phase 2.7: 絞り込みアプローチ採用)

**Phase 2.7 で全面書き直し: 名称マッチ廃止 + SitsumonNo 直接活用 + forward 経路探索**

軸特定ロジック:
1. 差分 step1.0 から **SitsumonNo を直接抽出** (旧版の名称マッチ廃止)
2. UI 可視判定 (SitsumonKind=17/19、ShortCutSitsumonNo 除外、MinKigou 内部分岐除外)
3. baseline flow_walker で訪問 Sit 確認
4. vary 候補が baseline 未訪問 → 前段 Sit の選択肢を **forward 試行**(single-probe)
5. 到達経路発見した probe Sit を **強制行ID 付き fix 軸**として登録
6. yaml axis_behaviors の業務ルール適用 (例: 資材計上区分 → vary 昇格)

出力: `step2.0_テスト計画.csv`
ヘッダー: 軸ID, 軸名, 種別, SitsumonNo, 列ラベル, 変更理由, 備考, **強制行ID** (新規追加)

---

## ③ 列形式テストケースCSV生成 (Phase 2.7: 強制行ID対応)

**Phase 2.7 で全面書き直し: 強制行ID 反映 + display_col 改善 + 差分/回帰判定**

生成ロジック:
1. step2.0 から「強制行ID」 を読み込み
2. fix/auto 軸: 強制行ID あり → その行で固定、なし → DefaultRowID
3. vary 軸: cartesian で TC 列挙
   - 旧/新JSON 比較で「追加 Row」 を特定し、追加 Row より上の既存 Row は除外
   - 業務ルール由来 vary 軸の「状態戻し回帰TC」 を 1件追加
4. 各 TC で flow_walker (強制行 + vary を vary_selections に投入)
5. TC walker で訪問された Sit のみ列に残す
6. display_col: 行ごとに unique 値が多い列を優先 (D 改善で VarName 持ち列も候補)
7. SitsumonKind=17 は「任意」 表記
8. 期待値 S1〜S5: 代価表行 inactive で空欄。任意入力軸あり+S=0 で「計算結果が正しいか」
9. テスト区分:
   - 追加 Row を選ぶ TC = **差分**
   - 業務ルール vary 軸 含む = **差分**
   - 業務ルール vary 軸の状態戻し TC = **回帰**
   - それ以外 = 回帰
10. 確認観点に「規格名計上の判断観点」 を vary 軸ごとに追加

### Phase 2.7 完了後の達成

| 工種 | TC | 差分/回帰 | 内容 |
|---|---|---|---|
| 32-4527 裏込砕石工 | 3 | 2/1 | 状態戻し回帰TC生成、ベースライン化済 |
| 02 大型ブレーカ | 1 | 1/0 | 修正対象「2014年規制」 を 1 TC に集約 |
| 03 送気用設備 | 1 | 1/0 | 任意+「86」 数値表記 |
| 04/05/06/07 | 1〜4 | 全差分 | フィードバック未収集 |
| 08 薬剤(Kg) | JSON投入待ち | - | - |

---

## エンジン構成要素

### Expression評価器 `expression.py`

`source_ref/sirius/Being/Core/Expressions/Parser.cs` 準拠の Python実装。

- Decimal主体・`^`のみ float
- 0除算 → 0、負底のべき乗 → 0、比較は 1/0
- `IF` / `&&` / `||` のショート評価
- `KeisanHyo.strict_undefined=True` で leaf未確定のみ厳格化

### flow_walker `flow_walker.py`

`FlowItems` を Start から traverse。

- Start: `FlowItemFlags=[0]`、真の終端: `CallBox=[-1]`
- Bunki遷移: 選択行 → CallBox[行index]
- 行選択優先: vary > AutoSel