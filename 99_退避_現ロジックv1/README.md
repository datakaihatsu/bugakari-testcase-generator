# 退避_現ロジックv1（凍結保存）

現ロジック＝**修正前JSON と 修正後JSON の差分からテストパターンを生成**するエンジンの凍結スナップショット（2026-06-25 時点）。

- **用途が違うため保存**しているもの。**原則として更新しない**（凍結）。
- 内部パスは自フォルダ名（`99_退避_現ロジックv1/`）で独立動作する。
- 機密 `source_ref/` は複製していない（`01_第1弾_運用化/` 側に1つ）。
- 共有資産（`工種別/`・`進捗報告/`・`docs/`）はプロジェクト直下を参照。

開発の現役は `01_第1弾_運用化/`、新ロジックは `02_第2弾_G条件/`。
