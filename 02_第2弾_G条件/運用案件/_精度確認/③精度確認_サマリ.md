# ③(G条件→改定後TC) 精度確認サマリ（2026-07-06）

## 方法
回帰targets(diffモード)の33工種で、20=gen_gjoken(改定前JSON)・30=gen_gjoken(改定後JSON=人作成の土台)
を生成→③実行→**99の合格TCと構造突合**（軸列・行数・テスト区分・入力値）。
**期待値(代価表行と数量)・観点文言・規格名計上は対象外**＝ユーザ確定の制限事項。
明細: `③精度確認_合格TC構造突合.csv` ／ ハーネス: `../_精度確認③_使い捨て.py`

## 結果: 構造完全一致 14/33
一致: 01回航費 / 04下地処理 / 06土のう / 12重建機 / 15プレビーム / 20ため池 / 23輸送費 /
28PC板仮置 / 34反転引込 / 35モルタル上塗 / 36床版足場 / 38FEP / 42解析調査 / 43捨石均し

## 差分あり19の分類

### L. 原理的制限（G条件に現れない改定）— 9工種
歩掛数値・計算表・質問設定(フロー/実行種別)のみの改定はG条件差分ゼロ→数値検証行が出ない。
**期待値を人が扱う運用（制限事項）と同根**。合格TCの「数値が変わった経路のvary行」が③には無い。
- 26目地工（G差分なし・合格11行は数値改定行）／ **44・45潜水士船（20↔30完全一致を確認済み**・
  質問設定/計算表変更）／ 00裏込砕石（列到達のみG可視、数値行3→1）／ 09養生マット・11ICT・
  25現場打水路・29PC板敷設・07IB(部分: 計上系4行分)

### C. 表記・軽微（実害小）— 4工種
- 13MC/MG・27横組: **質問名(Mesho)の文字変更を擬似JSONに未適用**→列名が改定前のまま（改善容易）。
- 37シーリング: (固定)サフィックス有無のみ。03送気用: 列順のみ（挿入位置）。

### B. 要改善 — 6工種
1. **21側溝: 欠落**（最優先）。選択肢追加→新規到達の「内径又は内空幅(mm)」列が出ない。
2. 41杭打: 改定後の新規質問ペア（計上区分+数量列）で計上区分が1択(固定)扱い・数量列欠落。
3. 02大型ブレーカ・40小型不整地: 選択肢追加の複製注入で、選ばれる行が合格と別規格になる(TC1のみ)。
4. 22路上路盤・05掘削・30型枠: 既存質問の新規到達列の余分（決定A=多め方針の範囲だが間引き余地）。
5. 24表層: 選択肢テキスト変更で1行余分。

## 本日の改良（この精度確認により実装済み）
- 新規到達列の判定: 30のみの列でも**質問が改定前JSONに存在→展開せず1値挿入**
  （＋選択肢に改定前に無いものがあれば真の改定として展開）。
- 単一選択肢の新規列は「(固定)」表記（04一致化）。数値入力列には付けない。
- gen_gjoken: 工種名の「/」等をファイル名でサニタイズ（13対応）。

## 評価
- **G条件が変わる改定**（第2弾の主対象）はおおむね正しく叩き台化できる。純粋ケースは一致。
- 不一致の主因はカテゴリL＝**G条件の守備範囲外**（数値・内部改定）。ここは第1弾ロジック
  （JSON差分）や人の観点でカバーする前提の設計。
- 実運用の30は人作成であり、本テストの30(機械生成の土台)より情報が増える方向。

## 注意
本テストの30_人作成G条件は改定後JSONからの機械生成（=加工の土台）を代用。
実運用では人が編集するため、列名・選択肢の表記が変わり得る（fuzzy一致で吸収する設計）。
