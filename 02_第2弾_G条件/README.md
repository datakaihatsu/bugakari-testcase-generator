# 第2弾_G条件（新ロジック・開発中）

第2弾＝**G条件を生成する新ロジック**のトラック。`01_第1弾_運用化/` 完成後に本格開発する想定で、現状はエンジン基盤を第1弾から複製した素地。

## 第2弾の考え方
- 既存（商品）歩掛JSONから **G条件を逆生成**する。G条件はJSONから正確に起こせる（構造化データ起点＝表記揺れなし）。
- G条件の出力フォーマット＝**「施工単価入力基準表」の様式**（赤本J条件に整合）:
  - 施工歩掛コード・施工単位 ＋ 入力条件 J1..Jn（条件名と区分①②…）
  - (注)＝**入力条件に影響する分岐ルールのみ**（例: J1=②ならJ3不要）。単価登録・数量算出は対象外（入力条件に効かない／単価はJSON外）。
  - J列＝エンジンの既存vary軸判定を流用。サンプル: `docs/02_第2弾_G条件サンプル.png`（06土のう積工 WB252620）。
- 人が改定後G条件を編集 → G条件からTC生成 → 改定後JSONと突き合わせ（乖離1.5拡張）。

## 位置づけ
- 本命トラック。完成後は第2弾を主とし、第1弾の更新は停止。
- 機密 `source_ref/`・`_backup_v1/` は複製していない（必要時に第1弾から参照/移送）。
- 共有資産（`工種別/`・`進捗報告/`・`docs/`）はプロジェクト直下を参照。

## 現在地（2026-07-06）
- **③(G条件→改定後TC)の精度確認 完了**: B類(要改善)全クローズ。35工種＝一致19＋許容差4＋制限L(G条件に
  現れない改定=守備範囲外)。詳細は `運用案件/_精度確認/③精度確認_サマリ.md`。
- **回帰テスト2本体制**: `regression/run_regression.py`(99パイプライン) と
  `regression/run_regression_tc3.py`(①G条件＋③TCのE2E回帰・35工種)。
  **gen_gjoken.py / gen_tc_from_gjoken.py を改修したらコミット前に必ず後者を実行**。
- **次フェーズ＝運用化**（検討→設計→実装）: ①=歩掛JSON格納場所からキー/コードで該当JSONを探して
  G条件生成（27.乖離チェックの探索ロジック踏襲）/ ③=人編集の改定後G条件(xlsx)→TC生成。
  UI=①③同居のローカルWebアプリ・境界のみxlsx(装飾なし)・内部/回帰はCSV維持・ロジック層とUI層分離。
  詳細方針は `運用化設計書_2026-07-07.md` 参照。
- 検証工程(乖離チェック=answer合わせ)は**第3弾**（改定後JSON模擬実行による合否・回帰自動化）へ移設
  （ロードマップHTML 2026-07-06版）。
