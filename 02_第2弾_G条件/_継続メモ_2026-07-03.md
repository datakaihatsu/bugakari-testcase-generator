# 第2弾 G条件生成 — 継続メモ（2026-07-03）

## 今日のテーマ
7-02メモ残タスク「③ まだ残る"本当の余分"＝計上/年度/諸元系」を**1件ずつ精査**し、
除外すべきか決定Aで許容か確定させた。

## 結論: ③ 全5件を精査 → 真に除外すべきは #1(年度切替用) だけ

| # | 項目 | 結論 | 根拠 |
|---|---|---|---|
| **#1** | 07 年度切替用(Sit76) | **除外（対応済）** | 使用年度区分(MIF_N=定数)による年度版切替の内部機構。ユーザ軸でない |
| #2 | 07 空練りモルタル計上区分(Sit12) | 決定A余分・残す | 敷材料の種類(Sit10)行5で開く分岐限定質問。baseline未到達 |
| #3 | 07 特殊IB単位選択(Sit17) | 決定A余分・残す | ブロック使用材料(Sit20)行5で開く分岐限定質問。baseline未到達 |
| #4 | 21 側溝単価(別途計上)(Sit12) | 決定A余分・残す | EK=1デフォルト実行auto。step3が"再選択可能"として列化する設計で **12工種が同カテゴリに依存**（隠すと02回帰12工種DIFF）。課題C=仕様通りでクローズ |
| #5 | 40 積込区分(Sit9) | 決定A余分・残す | 規格選択(Sit7)行8=クローラ式2.0t級でのみ開く。合格TCは0.7t級のみ→未到達（B-25「0.7経路で不要」と一致） |

→ **#2〜#5 は「合格TCが未テストの分岐で実際に開く正当な条件質問」**。決定A（多め・欠落回避・余分許容）で
  ユーザ承認済み(2026-07-03)。新規モードは #1 の内部機構を除き正しく動作、と裏取り完了。

## #1 の実装（唯一のコード変更・未コミット）
`step2_proposals/generate_proposals.py` に3メソッド追加 + `_is_ui_visible_axis` から呼出（+76行）:
- `_is_frozen_const_var(v)`: 変数が凍結定数か（KeisanItem Value有・式なし・上流SitCol設定なし・外部O~非依存）。
- `_var_mesho(v)`: KeisanItem Mesho取得。
- `_levelvar_frozen_autoselected(sit)`: レベル変数・行駆動変数がともに凍結定数 **かつ** ともに
  Mesho「使用年度」系（年度切替機構）で、実際に行が自動選択される → 除外。
  ※ 07 市場単価の日当り施工量補正有無(Sit28)は構造完全同型だが年度系でないので巻き込まない
    （合格TCでは固定列として残る）。構造だけでは区別不能→年度シグナルで限定。

### 検証
- **02回帰 40工種 完全一致（DIFF=0）** = 差分モード・新規モードbaseline(10/16/17)いずれも不変。
- 07突合: 年度切替用が消え、市場単価日当り(Sit28)は維持、欠落なし・一致列12。

## 重要な学び（構造だけで区別できない同型ペア）
07 Sit76 年度切替用 と Sit28 市場単価の日当り施工量補正有無 は
SitRows/SitCols/SitCells/SitTabRows/レベル定数2/EK まで**完全同型**。唯一の差は駆動変数の Mesho
（使用年度区分 vs 市場単価…）。→ 年度切替の意味シグナルでのみ分離可能。
内部仕様書 §1.5.1 に追記済（機密・ローカルのみ）。

## 未コミット状態
- `generate_proposals.py`（#1修正）… コミット可否はユーザ確認待ち。
- `歩掛JSON内部仕様書.md §1.5.1`（機密・git管理外）。
- 使い捨て: `_inspect_sit.py`（Sitノード詳細ダンプ・汎用/再利用可）, `_診断07_使い捨て.py`。

## ① 40 `条件選択`(Sit2) 欠落 → 解消（版差ではなく真の欠落だった）
- **版差説は誤り**: 無印/-d8bfd285 の両版とも同一G条件で両方とも条件選択を欠いていた。
- **真因**: Sit2 条件選択 は行駆動 `FG1`(運搬物種別。Sit40-48がSitColで設定)で開閉。
  `_is_ui_visible_axis` は「FG1は確定変数(上流が設定)」で**無条件除外**していた。
  だが行条件 Row4:FG1==9 / Row5:1<FG1<6 に対し FG1∈{1,6,7,8} は**範囲の隙間→開く**
  （合格TC: 運搬物種別=セメント→FG1=8→開く→"人力荷卸し"）。差分モードは
  `_opens_on_forced_route`(generate_proposals L837-868)で救済済だが**新規モードに無かった**。
- **実装** (`generate_proposals_new.py`):
  - `_discover_reachable` が各分岐走査の変数スコープを `self._branch_scopes` に収集。
  - `generate()` の UI非可視除外分岐で、いずれかの分岐スコープで
    `auto._opens_on_forced_route(sn, sc)` が真なら **vary で残す**。
  - **重複ガード2点**: (a)選択可能≥2行 (b)同名が既に可視軸なら補完しない
    （21 側溝規格=Sit33が既に可視→Sit34補完せず重複列を防止 / 12 リフター規格選択=1行を除外）。
- **検証**: 02回帰 40工種完全一致(DIFF=0)。40突合 一致列4→5(条件選択復活・欠落0)。
  副作用は 12 ラフテレーン規格選択(+1) のみ＝既存クローラクレーン規格選択と同カテゴリの
  機械規格分岐で一貫化（決定A・欠落回避に沿う）。21は重複回避で不変。

## 追加要求（同日）: G条件に「規格名計上」行を追加
Gaia入力基準表CSVに **規格名計上** 行を新設し、規格名/規格を代価表へ計上する条件(G列)に
○ を振る。判定は TC生成側 `_has_kikaku_keijo` を再利用。
行順(FB反映): 1=G番号 / **2=規格名計上** / 3=各種(条件名) / 4行目以降=選択肢
（条件名を選択肢の直上に隣接させ、つなぎを自然にする）。

### 実装 (`gen_gjoken.py`)
- 各G条件グループについて `gen._has_kikaku_keijo(sit)` が真なら ○（列出力に規格名計上行を追加）。
- **エコー帰属**: 列にならない自動確定エコー質問(例 12 機械区分Sit71=inGaia9)が規格名計上を
  持つ場合、その行駆動変数(例 J2)を **書き込む列(機械質量区分/各質量区分)** に ○ を付ける。
  `_vis=TestPlanGenerator` で「列になるか(UI可視)」を判定し、非可視の計上質問の駆動変数を
  `echo_kikaku_vars` に集約 → その変数を SitCol で書く列に付与。
- 精度検証: `_規格名計上突合_使い捨て.py`（9工種で 合格TC規格名計上列 vs G条件○）。

### 精度（合格TCとの矛盾＝0）
- 8/9工種: 合格TCが計上と観点化した軸は全てG条件で○（矛盾なし）。
- 12: 合格TCは「機械区分」(自動エコー)を計上と観点化 → G条件は駆動列「機械質量区分」等に
  ○（実体は同一。突合スクリプトは名前一致のため★表示するが**真の矛盾でない**）。
- **超過は正常**: 合格TCの規格名計上列は `is_diff_vary`(改定で変わった軸)のみ観点化する仕様。
  G条件は全計上条件を○にするため網羅的に多くなる（07: KikakuKeijoGaia9集合に作業区分/夜間
  作業補正等が実在。より完全な表示）。
- 02回帰40工種完全一致（TCパイプライン非改修・gen_gjokenのみ）。

## 未着手・残
- 特になし。次はコミット可否の判断。
