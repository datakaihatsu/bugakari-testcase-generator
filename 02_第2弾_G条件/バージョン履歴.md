# G条件・テストケース生成ツール バージョン履歴

配布物の版を受け渡し先と突き合わせるための履歴。
**バージョンの正は `webapp/version.py`**（画面ヘッダー右上に `v○.○.○ (YYYY-MM-DD)` で表示）。

採番: セマンティック ＋ ビルド日
- MAJOR … 使い方が変わる非互換変更
- MINOR … 機能追加（タブ追加・新モード等）
- PATCH … 不具合修正・生成ロジックの補正

リリース手順:
1. `webapp/version.py` の `APP_VERSION` / `BUILD_DATE` を更新
2. 本ファイルへ1行追記
3. 配布zipを差分リビルド（既存zipを展開→`app/` だけ最新masterで上書き→再zip）
4. 共有ドライブ `G:\共有ドライブ\Gaia開発部_データ開発課\社内共有(調整中)\99_その他\Tool\5.外部設計\G条件とテストケース作成\Gaia歩掛テストケースツール.zip` を差し替え
   （コード修正をリリースしたら必ずここも合わせて差し替える運用・2026-08-31確認）

| バージョン | ビルド日 | 内容 |
|---|---|---|
| v1.2.3 | 2026-09-02 | ③の「選択肢文字修正」行の確認観点に「文字修正後の選択肢を選んだとき、代価行・数量・単価が積算基準および設計書どおりであること（文字だけの修正か、内容の変更かを確認）」を1行追加（案C・ユーザ合意）。文字だけでは名前直しと別物への置き換えを判別できず、12 重建設機械分解組立では表記統一14件の中にしきい値 1.4m3→1.5m3 の実体変更が混在していたため、文字修正行でも計算結果まで確認させる。分類ロジック・TC行数・列数は不変。回帰: 文字修正行を持つ 12・22・24・27・30 の観点セルのみ文言差分（承認のうえベースライン更新）|
| v1.2.2 | 2026-09-02 | ③で条件名を変えて選択肢を全入替すると IndexError で落ちる不具合を修正（鉄枠固定ボルト材料費 160-1722「蓋区分」→「○区分」・協力会社報告）。原因は類似名を改名と判定→旧選択肢を先に全削除→新選択肢の複製元なしで0行。対処（ユーザ合意）: ①条件名は**完全一致のみ**同じ条件とし、名前が変わった列は別条件（削除列＋新規列）として全選択肢を網羅（テストは安全側） ②擬似JSON合成を「追加→削除」の順に（同名・全入替も通る） ③追加できない選択肢が残れば利用者向けメッセージで停止。回帰: 99全一致 / tc3は13・27が意図どおり変化（承認のうえベースライン更新）、他33一致 |
| v1.2.1 | 2026-09-02 | 組合せ上限ガードの**縮退を廃止**し、上限超の歩掛は「この歩掛は条件の組合せが多すぎるため生成できません」と明示して返す。v1.1.2 の縮退（基準+1軸ずつ変更）では G条件表の(注)を1サンプルから帰納し、必要な条件を「入力不要」と誤記していた（43 捨石本均し で実証・全組合せなら回帰ベースラインと完全一致）。上限は計測に基づき 1,000→**3,000**（43=1,200 は全組合せに復帰／消波ブロック製作=4,096〜16,384 は生成不可）。網羅性補完(gen2)だけが上限超の場合は補完を破棄し元の結果を使う。方針=普通サイズの歩掛が正しく出ることを優先（ユーザ決定）。回帰: run_regression 全一致 / tc3 35工種全一致（43のDIFF解消）|
| v1.2.0 | 2026-09-02 | 参照先（GaiaCloud データ）の格納場所を画面から変更できるようにした（協力会社1名が GaiaCloud データを外付けSSDへ移して運用しており、固定パスでは使えなかった要望）。ヘッダーの「変更」→ データフォルダ(DB)を1つ指定すれば ExpCD/Bugakari を自動判定。設定は `%LOCALAPPDATA%\GaiaKoshuTC\settings.json` に保存し、**ツール更新（zip入替）でも消えない**。既定は従来どおり `C:/ProgramData/CoBeing/GaiaCloud/DB`・「既定に戻す」で復帰。生成ロジックは無変更（回帰全一致） |
| v1.1.2 | 2026-08-31 | 組合せ爆発でフリーズする不具合を修正（65-546-6435-22 消波ブロック製作: vary軸27本で直積105万通り→「生成中」のまま停止）。3段対策: ①実体同一質問(同名+同選択肢)の軸を連動化 ②直積1,000件超で「基準+1軸ずつ変更」方式に自動縮退 ③生成300秒超過/縮退後も過大な場合はエラーを返して解放（webapp設定 `gen_timeout_sec`）。既存工種は回帰全一致（出力不変） |
| v1.1.1 | 2026-08-27 | 生成ロジック修正: ①改修後G条件で削除された条件をTCから除去し「削除された条件が表示されないこと」観点を付与 / ②差分行が無い場合も新規条件の選択肢を展開 / ③(注)パーサが選択肢名中の「」で切れる不具合を修正 / ④実数入力条件を起点にした注の「任意」を受理 / ⑤確認観点がある行から回帰観点の埋め草を除去（不具合#544444 鉄枠固定ボルト材料費）|
| v1.1.0 | 2026-08-26 | 画面ヘッダー右上にバージョン表示を追加（受け渡し先との版の同定用）。機能面は v1.0.1 と同一 |
| v1.0.1 | 2026-08-17 | (注)パーサ修正: 語尾ゆれ許容・名称優先解決・未解釈注のUI赤字表示（コミット `80a8e92`） |
| v1.0.0 | 2026-07-09 | 運用開始版。デモ後3要求対応（新規歩掛モード / 変更セル色付け / (設計メモ)欄） |
| （前史） | 2026-07-10 | 配布batの起動不具合修正（日本語入り複数行 `if()` ブロックを廃止）。※バージョン表記導入前 |
| （前史） | 2026-07-08 | 第三者配布用 完全自己完結zip 初版 |
