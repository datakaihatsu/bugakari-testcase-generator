# 第2弾 G条件生成 — 継続メモ（2026-07-05 / 07-06追記）

## ★★ アーティファクト運用ルール（2026-07-06 確定・厳守）
- ロードマップHTML等の**公開アーティファクト(claude.ai/code/artifact/98a97564-…)の更新はコード側(Claude Code)で行う**。
- **Coworkセッションから新規アーティファクトを作らない**（左上に別コピーができて混乱する。過去2回失敗）。
- Cowork側の作業は `docs/00共通_テスト自動化ロードマップ_2段階構造化.html` **ファイル本体の更新まで**。
  公開反映はユーザ/コード側に委ねる。

## ★ 2026-07-06(8回目) ③回帰テストの正式化（運用改修への備え・ユーザ合意）
- 新設: `regression/run_regression_tc3.py`＝**①G条件生成＋③TC生成のエンドツーエンド回帰**。
  diff全35工種で 20/30 G条件を生成→③でTC生成し、ベースライン
  (`進捗報告/regression_baseline/tc3/<id>/`＝git管理外)と**3ファイルをバイト比較**:
  `20_G条件.csv`・`30_G条件.csv`(=①gen_gjokenの回帰) / `step3.0_テストケース.csv`(=③の回帰)。
  G条件だけ変われば①起因・G条件同一でTCだけ変われば③起因、と層を切り分けられる。
- 使い方: 全件 `python3 02_第2弾_G条件/regression/run_regression_tc3.py`（約2〜3分。21が30秒超）
  / `--only 06,27` / `--range LO HI`（時間制限環境用）/ DIFFはユーザ承認後 `--update-baseline`。
- **運用ルール: gen_gjoken.py / gen_tc_from_gjoken.py を改修したらコミット前に必ず実行**
  （既存の02回帰=99パイプライン用と両方）。レポート=`進捗報告/regression_tc3_report_最新.md`(全件実行時)。
- ベースライン=本日の③精度確認完了時点(B類全クローズ)のスナップショット。全35工種で
  取得→再生成比較**全一致**(決定性も確認)。
- 背景: 実運用で細かい要求対応の改修が続く見込み→精度を固定する正式回帰が必要(ユーザ懸念合意)。
  制限L/許容差の運用者周知は木曜説明で対応予定(代価表の数値と行=制限事項として伝達)。

## ★ 2026-07-06(7回目) B-6(24)対処実装＝一致化 / B-7(27)3点修正＋許容差クローズ → **B類全クローズ**
- **B-6(#24)完了**: 前回対処案どおり実装。既定行の正体=`SitTab.DefaultRowID`(OLD=4→NEW=7)。
  renameペアの「(※標準)」移動を検出→擬似JSONの DefaultRowID を移動先行へ書換
  (`_fix_default_row_for_marker_move`。現既定=マーカー喪失行と一致する場合のみ=安全側)。**24=一致**。
- **B-7(#27)完了(許容差クローズ・ユーザ合意)**: 原因=SequenceMatcher.ratioの長さペナルティで
  注記追加型rename(「(床版桁を除く)」等の接尾辞)がたすき掛け(T桁⇔床版桁)に負ける。3点セットで修正:
  1. **接頭辞ルール**(`_prefix_bonus`): 旧が新の接頭辞(末尾括弧無視・逆も)なら最優先ペア。
     ※先に試した被覆率(一致文字率)優先案は22(20cm以下⇔0.2m以下)で誤爆し却下。
  2. **全選択肢が対象列を閉じるゲートは昇格しない**(run内by_src集計): 27注4/5の歩掛(有/無とも
     埋設型枠を閉じる=非到達経路でのみ開く)の誤vary昇格を排除。
  3. **純「選択肢テキスト変更」varyのゲート軸に業務ルール併記**(promote_gate_axes):
     step3の「既定行+最長行」剪定で床版桁経路が消える(正しいrenameだと最長行=既定行に縮退。
     現行はたすき掛けのおかげで偶然2行残っていた)のを回避。
     ※削除/追加混在varyは対象外(併記すると06の回帰行が変わるデグレ→flow_equiv代表行が正)。
  - 結果: 軸9本完全一致・床版桁経路＋埋設型枠展開を復元。残差=①床版桁×施工費のみ1行余分
    (注6「施工費のみ→埋設型枠閉」の閉じ側を検証できる唯一の行=過剰側で正当。人はコスト判断で省略)
    ②TC3/4行順 ③回帰行の選び方(合格=床版桁側/生成=T桁側)。**多い分はリスク低で残して様子見**。
  - 判断材料一式: `運用案件/_精度確認/27_B7判断材料/`(20/30 G条件・合格TC・現行/修正後生成TC)。
- 検証: 35工種突合=**一致19/35**(24追加・既存一致すべて維持・06もデグレ解消確認済み)。
  **02回帰40工種完全一致**(2回実施)。
- ※Edit末尾truncateバグが gen_tc_from_gjoken.py と本メモで再発(Write全文でも切れた)→
  **長いファイルはbash(catヒアドキュメント/pythonパッチ)で編集・必ずwc/tail確認**。
- B類はこれで全クローズ。未コミット(本日分): gen_tc_from_gjoken.py / 27_B7判断材料/ /
  ③精度確認_合格TC構造突合.csv / 本メモ。コミット可否はユーザ確認待ち。

## ★ 2026-07-06(6回目) B-6(24表層)原因特定・対処は次回
- 現象: ③が2行(基準行=未対策+変更検証行=第3次(※標準))、合格は1行(第3次のみ)。
- 調査: Sit8はAutoSelect/デフォルトフラグ無し・セルは擬似=実改定後と**byte一致**でも基準行が違う。
  実改定後はフローボックス全面付け替え(被災地Sit62挿入)。→ **既定行(※標準)はフロー側に保持**されており、
  G条件にはラベル内「(※標準)」の文字でしか写らない。擬似JSON(改定前フロー)は既定行が旧のまま→1行余分。
- 次回対処案: renameペアの「(※標準)」移動を検出→擬似JSONのフロー既定行を書き換え
  (FlowItemsの該当フィールド特定から)。余分1行は旧デフォルトの回帰確認として安全側=害小。
- 副産物(コミット対象): 選択肢rename時に**セルの改行(\r\n)構造を保存**する修正
  (`_rename_preserving_newlines`)。従来はG条件の正規化ラベルで1行に潰していた(構造破壊の予防)。
  06/02/03/21デグレなし。
- B残: 24(フロー既定行・対処案あり)・27(同名系列ペアリング)。

## ★ 2026-07-06(5回目) B-5続き: 05の改善・22の整理
- **05掘削等 4行→2行に改善**: 原因=吹付プラント設備の誤展開。改定前JSONに説明ラベル列が無く
  `_g_options` が別列(B/CⅠ…)を拾い、30の「一括練混ぜ/分割練混ぜ」と全く重ならず
  superset判定が「選択肢追加」と誤爆していた。→ **「旧選択肢と1つも重ならない場合は
  比較不能=展開しない(安全側)」**を追加(gen_tc_from_gjoken)。
- 05の残差(許容): ①「ドリルジャンボ排ガス1次対策型規格(固定)」列=**複製注入行がクローン元
  (第1次基準値)のフロー接続を引き継ぎ、偽の新規到達**を作る近似の限界。②コンクリート吹付機の
  展開1行=真の新規質問だが実は2021年追加のstale(旧JSONが2020年と古いケース特有。
  staleness=SitsumonVersionはG条件に写らない=制限)。
- **22路上路盤(残・許容)**: 砂の散布/砂使用量=新規到達の決定A余分(1値挿入・観点付き)。
  スタビライザ/タイヤローラの(固定)表記差=cosmetic。
- 検証: 06/30/21/00 デグレなし。
- **B-5クローズ**。B残=24(テキスト変更1行余分)・27(同名系列ペアリング)のみ。現況一致18/35。

## ★ 2026-07-06(4回目) B-4クローズ / B-5: 30型枠工の根本修正(SBK問題)
- **B-4(02/40)**: 02は複製注入先を「先頭グループのcanonical sit」に限定して一致化(3回目参照)。
  40の残TC[1]は「既存行のラベル書換+旧ラベル行追加」というRowID玉突きがラベル集合に写らないため
  **許容差としてクローズ**(ユーザ合意。③は追加に見える②10t当りを検証=G条件として正しい読み)。
- **B-5(30型枠工)=ユーザのSBK着眼で根本解決**:
  - 現象: G条件30に「施工歩掛」列が出る(合格TC/実Gaiaでは出ない)。
  - 真因: 施工歩掛の行はAutoSelectJoken(SBK≦1/≦2)で自動確定。SBKはSit1(規格)が直前に1/2へ
    上書きするため実ルートで隙間なし。しかし新規モードの#40補完(_opens_on_forced_route)が
    **分岐走査の最終スナップショット**(省庁区分=3がSBK=3を書き、その分岐はSit1/施工歩掛に
    到達しない)を見て「隙間=開く」と誤発火していた。
  - 修正(generate_proposals_new): `_discover_reachable`で分岐ごとの**到達質問集合を
    スコープと対で保存**(`_branch_visited`)し、補完は「**同じ分岐でsnに到達している場合のみ**」許可。
  - 検証: 30=③一致化・G条件から施工歩掛消滅 / **#40条件選択は維持(デグレなし)** /
    **02回帰40工種 完全一致(新規モードbaseline 10/16/17含めDIFFゼロ)** / 06/02/15一致維持。
- 05(ドリルジャンボ規格・行4/1)・22(砂の散布/砂使用量)は**SBK型とは別ルート由来で残**。次回B-5続き。
- 現況: **一致18/35**。B残=05・22(新規到達の余分)→24(1行余分)→27(同名ペアリング)。
- ※30比較フォルダのCSVはExcelロックで再生成失敗(古い=施工歩掛あり版のまま)。閉じてから再生成要。

## ★ 2026-07-06(3回目) #21要望: G条件の注の統合(読みやすさ)
- 同一ソース列・同一対象列の注は**1行に統合**(gen_gjoken):
  2件=「①「A」・②「B」のいずれかを選択した場合は…」/ 連番3件以上=「①「A」～③「C」のいずれか…」。
  例 21改定後: 旧注4+5(側溝規格①/②→G8·G9·G10)が1本に(14→13本)。
- ③側 `read_gjoken` は統合形式を**選択肢ごとに展開して**パース(～はopts範囲展開)。下流ロジック不変。
- 検証: 06のG条件不変(統合対象なし)・③突合 06/02一致維持・21はTC[3](既知の制限)のまま。
  21比較フォルダは新形式で更新済み。**精度確認10工種CSVは旧形式のまま**(改定前由来。
  再生成する場合は各CSVの元JSON版(改定前)に注意。次回コミット時に更新判断)。

## ★ 2026-07-06(3回目) B-3(02/40 選択肢追加の系列ズレ)
- 原因: G条件は同名系列列をマージし選択肢も汎用ラベル化→**「どの系列(sit)に追加されたか」を持たない**。
  ③は全系列に複製注入していた(02: 81〜85全部)→step3が4000系列で差分行を作り合格(1300)とズレ。
- 99の実差分は sit82(1300)のみ→probe経路が1300を選ぶ。**系列特定はG条件から原理的に不能**。
- 対処(ユーザ承認=案1): 注入先を**先頭グループのcanonical sit 1つに限定**
  (build_pseudo_json。02のグループ構造 [82,83,84,85]+[81] の先頭=82=1300で合格と一致)。
  観点に「追加された系列(規格)がGaia条件と一致しているか確認」を追記。
  試行錯誤メモ: 基準行到達での限定は**sibling補完で全系列に値が入る**ため機能しない。
- 結果: **02一致化**。40は残TC[1]（合格TC1行目が“旧選択肢”の行=系列問題と別性質。次回調査)。
  01/06/21に副作用なし(21のTC[3]=0.28割引セルは既知の制限のまま)。

## ★ 2026-07-06(2回目) B-2(41杭打)の調査と対処
- **B-2は制限Lに再分類**: 41の改定=数値質問(数量Sit53)の内部行削除＋計算表変更。20↔30のG条件CSVは
  注含め完全一致(diff0)＝③の入力に判断材料なし。合格TCの3行はJSON差分由来で原理的に再現不能。
- ただしユーザ指摘で**G条件の網羅性の粗**を発見: 再選択可能な計上区分(EK1デフォルト実行auto)の
  非既定選択肢で開く数量質問がG条件に出ていない(施工単価入力基準表として不完全)。
- **対処実装(gen_gjoken)**: `_promote_reselectable_for_coverage`＝到達済み(基準行で値を持つ)
  再選択可能auto(Kind19・選択可能行>=2・全行AutoSelectでない)をG条件導出用planでvary昇格→再導出。
  **安全弁**: 昇格後に列/選択肢が1つでも失われる場合は昇格を破棄(増える方向のみ許容)。
  fix昇格は07で列構成が崩れたため対象外。analyzeは `_derive_glist()` に導出部を分離。
- 検証: 41=数量列＋注(計上しない→数量不要)が出る(11列)。06=完全一致。02回帰=全一致(パイプライン不変)。
- **★07の「既存乖離」疑い → 調査の結果、乖離なし(誤検知)と確定(2026-07-06)**:
  コミット済み精度確認CSV(14列)は**改定前(2025)JSON**由来で、現行コードで再生成すると完全一致。
  私の比較が**改定後(2026)JSON**からの生成(13列)と突き合わせていたのが誤り。
  07の2026改定=空練りモルタル選択肢の削除なので、改定後で空練り列が消えるのは正。
  gen_gjokenは全コミット時点で一貫(worktree bisectで確認)。決定A(#2)とも整合(改定前の話)。
  ※教訓: 精度確認フォルダのG条件は「改定前JSON由来」。比較時はJSONの版を必ず確認。

## ★ 2026-07-06(2回目セッション): C類完了・B-1(21側溝)完了
- 方針(ユーザ): **C類→B類の順。B類は1件ずつ 現象再確認→原因究明→方針決定→実装**。
- **C類完了**: C-1 列名(Mesho)変更を擬似JSONへ適用(13/27→13一致化。27は同名系列の
  ペアリング誤りが露見→B類へ)。C-3 新規列が30先頭の場合の挿入位置(03)。
  数値新規列のTC値=「任意」・区分=差分化(状態戻し回帰行は除く)→03/04一致化。
  37は「注に現れない内部改定(補正値1.01→1)」＝制限Lへ再分類。
- **B-1(21側溝)完了**: 原因=追加選択肢「各種」で内径が開く情報が改定前JSONに無い。
  ユーザと合意した方針=**30の注ベースの再評価**を実装(`reopen_added_choice_rows`)。
  ルール: 追加選択肢のある列Xについて「既存選択肢すべてがYを閉じる注を持ち、追加選択肢
  だけ注が無い」→ Yを開くと確定し、追加選択肢行をYの選択肢数に展開(閉じ方向は不変更)。
  ※証拠が揃わない場合は展開しない(40の運搬物ルート排他の誤爆で学習→厳格化)。
  結果: 21は内径列＋3行構造を復元、残差はTC3の0.28割引1セルのみ
  (内径400超600→0.28閉は**注に現れないゲート**=制限)。
- 突合ハーネスを**名前ベース行比較**(列順差は「一致(列順差)」)に改良。
- 現況: **一致16/35**、制限L≈10、B残: 41(新規質問ペア)→02/40(複製注入の行ズレ)→
  22/05/30(決定A余分)→24(余分1行)→27(同名ペアリング)。02回帰40工種一致・06一致維持。
- 未コミット(累計): gen_gjoken.py / gen_tc_from_gjoken.py / _精度確認③_使い捨て.py /
  運用案件(_精度確認・06の20/30更新・60叩き台) / メモ類。

## ★ 2026-07-06 追記: 期待値列の仕様変更（ユーザ指示・対応済み）
- 内部数値の改定があると改定前ベース値は保証できない → **「期待:変数」列を全廃**。
- 同位置に **「代価表行と数量(数率)」列を1本**追加:
  - 1行目=「積算基準および設計書通りの代価行と数量が計上されていること
    ※ただし計上区分の切り替えによる代価行変動がある場合は、設計者が追記すること」
  - 2行目以降=「〃」（全行同文による麻痺を防ぐ差別化）。
- 「選択肢の適切さ確認」から以下2行を**削除**（◯仮置き運用の廃止に伴い不要）:
  「・期待値は改定前ベースの◯仮置き…(要人訂正)」「・追加された代価表行と数量(数率)が外部設計に沿っているか」。
- **合格TCとの一致度測定から期待:変数は除外**（構造=軸列・行・テスト区分・入力値で測る。
  06土のうは同基準で完全一致のまま）。
- 実装: `gen_tc_from_gjoken.py` の `replace_expected_columns()`。06の60_改定後TC叩き台は再生成済み。
- 07-05メモ中の「◯仮置き＋要人訂正」の記述は**本仕様に置き換え**て読むこと。

### 同日追加対応（土のう工の指摘3件）
1. **規格名計上の欠落を解消**: 原因=06の20/30 G条件CSVが旧フォーマット(規格名計上行なし)。
   両CSVを現行フォーマットで更新(それ以外は完全一致を確認の上)。→「土のう種類の規格名計上」観点が
   合格TCどおり出力されるようになった(制限4は解消)。
2. **観点の文言**: 「外部設計と正しいか」→**「Gaia条件と一致しているか」**(step3生成行も含め
   `fix_kanten_wording()` で置換)。
3. **出力ファイル名**: `step3.0_テストケース_<工種名>.csv`(工種名=Daika DaikaTitle)。

### 同日: ③の多工種精度確認（33工種）
- 回帰targets(diff)33工種で 20/30を機械生成→③→**合格TCと構造突合**（期待値は対象外=制限事項）。
- **構造完全一致 14/33**。差分19の分類・改良内容・評価は
  `運用案件/_精度確認/③精度確認_サマリ.md`（明細CSV同フォルダ）を参照。
- 主因: L=G条件に現れない改定(数値/計算表/質問設定)9件（44/45は20↔30完全一致を確認）＝設計上の守備範囲外 /
  C=表記・列名rename未適用4件 / B=要改善6件（**最優先=21側溝の新規到達列欠落**、41新規質問ペア、02/40複製注入の行ズレ）。
- 改良実装: 新規到達列(質問が改定前JSONに存在)は展開せず1値挿入＋選択肢superset判定 /
  単一選択肢新規列=(固定)表記 / gen_gjokenファイル名サニタイズ。02回帰40工種一致・06一致は維持。

## 今日のテーマ
**③「G条件 → 改定後TC」生成に着手・v1完成**（起点: `_次回実装メモ_③G条件→改定後TC.md`）。

## ユーザ確定事項（本日）
- 新規追加列（例: 土のう種類）の期待値 = **改定前ベースの◯仮置き（値ではなく◯=計上あり）＋要人訂正マーク**
  （空欄と改定前値の中間案。観点欄に「実値は人が確認・記入すること(要人訂正)」を明記）。
- 既存軸の期待値 = 改定前JSONのKeisanHyo評価値（改定で変わる値は人が訂正する運用、従来どおり）。

## 実装（新規: `gen_tc_from_gjoken.py`）
使い方: `python3 gen_tc_from_gjoken.py <20_叩き台CSV> <30_人作成CSV> <改定前JSON> [出力dir]`
（出力dir省略時 = `<工種>/60_改定後TC叩き台/`）

方式（実験で裏取り済み）:
1. **G条件CSV差分**: 列=名前マッチ(完全→類似)、選択肢=完全一致→difflib類似ペア=文字変更、
   20のみ=削除、30のみ=追加。30のみの列=新規追加質問。注=正規表現でゲート(ソース列/選択肢/対象列)を抽出。
2. **擬似改定後JSON**: 既存質問の選択肢削除・文字変更を改定前JSONの Sitsumon019
   (SitRows/SitTabRows/SitTabCells) へ適用。列⇔質問Noは `gen_gjoken.analyze()`（本日リファクタで共有API化）。
3. **99パイプライン再利用**: step1(改定前→擬似)→step2→step3。期待値は擬似JSON=改定前ベースで自動算出。
   - **vary昇格**: 30の注で新規列をゲートする既存条件を step2計画CSV上で
     `業務ルール(G条件ゲート)` として vary 昇格 → 全選択肢網羅＋**状態戻し回帰TC**が出る（合格TCと同型）。
4. **新規追加列の後処理**: 30の選択肢で差分行を展開・注ゲートで"-"・状態戻し回帰行は先頭選択肢。
   展開行の期待値=◯仮置き＋観点「(新規追加)/表示確認/要人訂正」。規格名計上○(30に行がある場合)も追記。

## 検証結果
- **06土のう積工: 99合格TCと構造完全一致**（軸列5・行5・テスト区分差分4+回帰1・全入力値一致）。
  期待:列は S1,S2,S3（改定前枠）。合格TCのみにある S4,S5 は**改定追加代価行由来→人が追記する枠**（設計どおり）。
- **02回帰: 40工種 完全一致**（gen_gjoken リファクタの副作用なし。リファクタ前後で06のG条件出力も完全一致を確認）。

## gen_gjoken.py のリファクタ（出力不変）
- `build_g()` から解析部を `analyze(json_path) -> (bj, gen, g_list, notes)` に分離。
  `g_list` に `'sits'`（列を構成する質問Noリスト）を追加。③から列⇔質問No対応を得る共有API。
- ※Edit末尾truncateバグが2回発生 → bash(head+cat)で復元済み。このファイルの編集は要注意。

## v1の既知の制限（次回改善候補）
1. **擬似JSON編集のラベル照合はcanonical Sitのラベル前提**。同グループの別変形Sit
   (例 06のSit3「土のう拵え,積立,撤去」)はラベル不一致で未編集のまま
   （基準ルートがcanonical側なら結果に影響なし。厳密化するなら per-sit の _g_options 類似対応で適用）。
2. **選択肢追加（既存質問）**: 最類似行の複製でJSONへ注入し、該当行の期待値を◯仮置き＋要人訂正
   （歩掛値は複製元のまま＝仮）。実データ未検証。
3. **数値入力(実数入力)の新規列**: 展開せず「(実数入力)…」1行のみ。実データ未検証。
4. 30が旧フォーマット（規格名計上行なし）の場合、新規列の規格名計上観点は出ない
   （06人作成が旧フォーマットのため合格TCの「土のう種類の規格名計上」観点は今回未出力）。
5. 観点の年次表記: 合格TC「(2026年新規追加)」に対し「(新規追加)」（年次はG条件から取れないため）。

## 未コミット
- `gen_gjoken.py`（analyze分離リファクタ）
- `gen_tc_from_gjoken.py`（新規・③本体）
- `運用案件/06_土のう積工/60_改定後TC叩き台/`（生成物4ファイル）
- 本メモ。**コミット可否はユーザ確認待ち**。
- 掃除: `_gen_head_tmp.py`（HEAD版比較用の一時ファイル。削除してよい）

## 次の一歩
- 他工種（新規列・選択肢追加のある工種）で③を試し、制限1〜3を実データで検証。
- ③の乖離チェック（README定義の③: 20↔30差分 × 改定後JSON実装差分の照合=answer合わせ）は別途未着手。
